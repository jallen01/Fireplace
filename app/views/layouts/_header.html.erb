<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <div class="container">
      <ul class="nav nav-fireplace">
        <%= link_to (image_tag('favicon.ico', class: "logo")+" Fireplace").html_safe, root_path, :class => 'brand' %>
      </ul>
      <ul class="nav pull-right">
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown">
            Menu <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% if user_signed_in? %>
              <li>
                <%= link_to('Tags', tags_path(current_user)) %>
              </li>
              <li>
                <%= link_to('Locations', locations_path(current_user)) %>
              </li>
              <li>
                <%= link_to('Fireplace', tasks_path(current_user)) %>
              </li>
            <% end %>
            <% if user_signed_in? %>
              <hr>
              <li>
                <%= link_to('Edit Account', edit_user_registration_path) %>
              </li>
            <% else %>
              <li>
                <%= link_to('Sign Up', new_user_registration_path)  %>
              </li>
            <% end %>
            <% if user_signed_in? %>
              <li>
                <%= link_to('Logout', destroy_user_session_path, :method=>'delete') %>
              </li>
            <% else %>
              <li>
                <%= link_to('Login', new_user_session_path)  %>
              </li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</div>