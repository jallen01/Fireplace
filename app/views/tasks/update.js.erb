<% unless @task.errors.any? %>
	$(".task-list").html("<%= j render 'tasks/list' %>");
	$("#eidt-task-#{@task.id}-modal").modal('hide');
<% end %>

$("#new-task-modal modal-body").html("<%= j render 'tasks/form', task: @task %>");