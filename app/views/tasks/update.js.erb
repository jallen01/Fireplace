<% unless @task.errors.any? %>
    $("#tasks-list .list-group").html("<%= j render "tasks/list", task: @task%>");
    $("<%= j "#edit-task-#{@task.id}-modal" %>").modal("hide");
<% end %>

$("<%= j "#edit-task-#{@task.id}-modal .modal-body" %>").html("<%= j render "tasks/form", task: @task %>");

