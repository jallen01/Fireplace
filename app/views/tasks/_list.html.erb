<!-- Tasks list -->
<div class="list-group">
  <% @tasks.each do |task| %>
    <%= link_to task.title, "#edit-task-#{task.id}-modal", data: { toggle: "modal", important: !!task.important, quick: !!task.long_lasting }, class: "list-group-item" %></td>
  <% end %>
</div>
