<!-- Tasks list -->
<div class="list-group">
  <% @tasks.each do |task| %>
    <%= link_to task.title, "#edit-task-#{task.id}-modal", data: { toggle: "modal" }, class: "list-group-item" %></td>
  <% end %>
</div>
